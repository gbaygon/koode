// genius access token to access lyrics
export const GENIUS_ACCESS_TOKEN= 'SET_YOUR_ACCESS_TOKEN_HERE';

// url or ip of moode
export const moodeServer = 'http://moode.local';

// temporary screenshot file
export const screenshotFile = '/tmp/screenshot.png';

// folder where to save the lyrics
export const lyricsCache = './lyrics';

// port where to run the server
export const port = 8080;

// template server, tipically this same server in '/'
export const templateServerAddr = `http://127.0.0.1:${port}`;
